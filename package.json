{
	"name": "nyanko-typing-kaplay",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"deploy": "npm run build && wrangler pages deploy",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"@kaplayjs/crew": "^2.1.0",
		"kaplay": "^3001.0.19"
	},
	"devDependencies": {
		"@types/node": "^24.12.0",
		"vite": "^7.3.1",
		"wrangler": "^3.45.0"
	}
}
